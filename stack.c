#include "monty.h"
/**
 * f_stack -Will  print the top of stack
 * @head: Top of stack
 * @count: number
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
	bus.lifi = 0;
}
